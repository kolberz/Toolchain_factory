# Frontier 25 — Native WTC Cost Bridge + Linear Residue Transition

This frontier re-kernel-checks the exact retained Frontier-23 WTC/CNF/LRAT source closure, then imports the Frontier-24 fixed indexed state. It replaces the quadratic predecessor scan with one modular predecessor lookup per destination, proves the declared full-materialization work equality `n*m`, bridges the direct state to Frontier-23 `reachableResidues`, adds proof-carrying costed WTC obstructions and a generic argmin theorem, and preserves the GCD → residue → LRAT strategy order.
