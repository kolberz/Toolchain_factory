# V89 CI Probe Scope

This branch exists only to make the research-branch pull-request workflow run discoverable through the GitHub connector. It does not modify Lean proof source and targets `research/v89-clause-history`, not `main`.
